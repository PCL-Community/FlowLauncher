<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <OutputType>WinExe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <Nullable>enable</Nullable>
        <LangVersion>latest</LangVersion>
        <ImplicitUsings>true</ImplicitUsings>
        <ApplicationManifest>app.manifest</ApplicationManifest>
        <AvaloniaUseCompiledBindingsByDefault>true</AvaloniaUseCompiledBindingsByDefault>
        <AvaloniaVersion>11.3.14</AvaloniaVersion>
        <PublishSingleFile>true</PublishSingleFile>
        <PublishReadyToRun>true</PublishReadyToRun>
        <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
        <PublishType>normal</PublishType>
    </PropertyGroup>

    <!-- Publish Type Options -->
    <PropertyGroup Condition="'$(PublishType)' == 'normal'">
        <DefineConstants>$(DefineConstants);FL_PUBLISH_NORMAL</DefineConstants>
        <SelfContained>false</SelfContained>
    </PropertyGroup>
    <PropertyGroup Condition="'$(PublishType)' == 'standalone'">
        <DefineConstants>$(DefineConstants);FL_PUBLISH_STANDALONE</DefineConstants>
        <SelfContained>true</SelfContained>
        <PublishTrimmed>true</PublishTrimmed>
    </PropertyGroup>
    <PropertyGroup Condition="'$(PublishType)' == 'native'">
        <DefineConstants>$(DefineConstants);FL_PUBLISH_NATIVE</DefineConstants>
        <PublishAot>true</PublishAot>
        <!-- Necessary before Avalonia 12.0, was used for accessiblity APIs -->
        <BuiltInComInteropSupport>false</BuiltInComInteropSupport>
    </PropertyGroup>

    <!-- Debug Symbol Options -->
    <PropertyGroup Condition="'$(Configuration)' == 'Debug'">
        <DebugType>full</DebugType>
        <DebugSymbols>true</DebugSymbols>
    </PropertyGroup>
    <PropertyGroup Condition="'$(Configuration)' == 'Release'">
        <DebugType>none</DebugType>
        <DebugSymbols>false</DebugSymbols>
    </PropertyGroup>

    <!-- Platform Options -->
    <PropertyGroup Condition="'$(TargetPlatformIdentifier)' == 'windows'">
        <DefineConstants>$(DefineConstants);FL_PLATFORM_WINDOWS</DefineConstants>
    </PropertyGroup>
    <PropertyGroup Condition="'$(TargetPlatformIdentifier)' == 'linux'">
        <DefineConstants>$(DefineConstants);FL_PLATFORM_LINUX</DefineConstants>
    </PropertyGroup>
    <PropertyGroup Condition="'$(TargetPlatformIdentifier)' == 'osx'">
        <DefineConstants>$(DefineConstants);FL_PLATFORM_MACOS</DefineConstants>
    </PropertyGroup>

    <!-- Manual File Includings -->
    <ItemGroup>
        <AvaloniaResource Include="Assets\**"/>
    </ItemGroup>

    <!-- Global Usings -->
    <ItemGroup>
        <Using Include="FlowNet.Core"/>
    </ItemGroup>

    <!-- Packages -->
    <ItemGroup>
        <PackageReference Include="Avalonia" Version="$(AvaloniaVersion)"/>
        <PackageReference Include="Avalonia.Skia" Version="$(AvaloniaVersion)"/>
        <PackageReference Include="Avalonia.Win32" Version="$(AvaloniaVersion)" Condition="'$(TargetPlatformIdentifier)' == 'windows'"/>
        <PackageReference Include="Avalonia.X11" Version="$(AvaloniaVersion)" Condition="'$(TargetPlatformIdentifier)' == 'linux'"/>
        <PackageReference Include="Avalonia.Native" Version="$(AvaloniaVersion)" Condition="'$(TargetPlatformIdentifier)' == 'osx'"/>
        <PackageReference Include="Avalonia.Desktop" Version="$(AvaloniaVersion)" Condition="'$(TargetPlatformIdentifier)' != 'windows' and '$(TargetPlatformIdentifier)' != 'linux' and '$(TargetPlatformIdentifier)' != 'osx'"/>
        <PackageReference Include="Avalonia.Themes.Fluent" Version="$(AvaloniaVersion)"/>
        <PackageReference Include="Avalonia.Fonts.Inter" Version="$(AvaloniaVersion)"/>
        <PackageReference Include="Avalonia.Diagnostics" Version="$(AvaloniaVersion)">
            <IncludeAssets Condition="'$(Configuration)' != 'Debug'">None</IncludeAssets>
            <PrivateAssets Condition="'$(Configuration)' != 'Debug'">All</PrivateAssets>
        </PackageReference>
        <PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.2"/>
        <PackageReference Include="FlowNet.Core" Version="0.2.2"/>
        <PackageReference Include="FlowNet.Collector" Version="0.1.2"/>
        <PackageReference Include="CmlLib.Core" Version="4.0.6"/>
        <PackageReference Include="SharpZipLib" Version="1.4.2"/>
    </ItemGroup>

    <!-- Localization -->
    <ItemGroup>
        <EmbeddedResource Update="Resources\Strings.resx">
            <Generator>PublicResXFileCodeGenerator</Generator>
            <LastGenOutput>Strings.Designer.cs</LastGenOutput>
        </EmbeddedResource>
        <EmbeddedResource Update="Resources\Strings.*.resx">
            <DependentUpon>Strings.resx</DependentUpon>
        </EmbeddedResource>
        <Compile Update="Resources\Strings.Designer.cs">
            <DesignTime>True</DesignTime>
            <AutoGen>True</AutoGen>
            <DependentUpon>Strings.resx</DependentUpon>
        </Compile>
    </ItemGroup>
</Project>
